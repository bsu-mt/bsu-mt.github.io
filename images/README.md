Screenshots referenced from `index.html`, one or more per card:

- `revitqc1.png` .. `revitqc4.png` — RevitQC (ribbon, check results, AI Copilot)
- `revit-hololens-pipeline1.png` .. `revit-hololens-pipeline3.png` — Revit → Unity → HoloLens 2 pipeline
- `robotstudio-quest-pipeline*.png` — RobotStudio → Unity → Quest 2 pipeline (not added yet)
- `wiztreeanalyst.png` — WizTreeAnalyst

Recommended: 16:9, at least 800px wide. Add more numbered files (e.g. `robotstudio-quest-pipeline1.png`, `...2.png`) and wire them into the `.project-gallery` block for that card — single-image cards use `<img class="project-shot">` instead.
